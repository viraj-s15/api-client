import { Component } from "solid-js";

const Hero: Component = () => {
  return (
    <div class="flex w-full m-6 rounded-xl text-center h-[94vh] text-black bg-gray-100 ">
      This is the main Component and must appear throughout the rest of the
      page. Its state will be managed soon
    </div>
  );
};

export default Hero;
